# gitlab-runner.msi
Track `gitlab-runner` updates on official repo and make appropriate `.msi` from original release.

## install
```cmd
msiexec /qn /b https://github.com/darkmsi/gitlab-runner/release/latest/gitlab-runner.msi
```
